#Instalar docker desktop
 Busca en google e instalas

#Ver la versión de docker
 docker --version

#Ver los procesos que hay o dockers que se estan ejecutando
 docker ps

#Lanzar el contenedor instalado
 docker run -it nombre_contenedor(kalilinux/kali-rolling)

#Entrar a un commit 
 docker run -it nombre_commit(kali-linux-install-vim)

#Realizar un commit
 docker commit id_SO nombre_commit
 ejemplo: docker commit e2625f76de71 kali-linux-install-code
 
